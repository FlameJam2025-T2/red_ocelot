// in percentages of the screen
const double shipSize = 0.05;

// max velocity in percentages of the world size
const double shipMaxVelocity = 0.005;

// acceleration (% of max velocity)
const double shipAcceleration = 0.75;
// deceleration (% of max velocity)
const double shipDeceleration = 0.5;

// rotation speed (radians per second)
const double shipRotationSpeed = 3.0;

const double mapSize = 5000;
